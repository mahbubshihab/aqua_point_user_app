import 'package:flutter/material.dart';

abstract class AppColors {
  /// Deep Obsidian `#0F172A`
  static const Color background = Color(0xFF0F172A);

  /// Frosted Slate `#1E293B`
  static const Color cardBackground = Color(0xFF1E293B);

  /// Electric Aqua Cyan `#00BCE1`
  static const Color primary = Color(0xFF00BCE1);

  /// Vibrant Royal Blue (`0xFF3B82F6`)
  static const Color secondary = Color(0xFF3B82F6);

  /// Accent Cyan (`0xFF00BCE1`)
  static const Color accentCyan = Color(0xFF00BCE1);

  /// Emerald Glow (`0xFF10B981`)
  static const Color accentGreen = Color(0xFF10B981);

  /// Warm Gold (`0xFFF59E0B`)
  static const Color accentGold = Color(0xFFF59E0B);

  /// Yellow accent alias for backward compatibility
  static const Color accentYellow = Color(0xFFF59E0B);

  /// Bold Crisp White (`0xFFFFFFFF`)
  static const Color textPrimary = Color(0xFFFFFFFF);

  /// Soft Slate Grey (`0xFF94A3B8`)
  static const Color textSecondary = Color(0xFF94A3B8);

  /// Input background fill color (`0x991E293B`)
  static const Color inputFill = Color(0x991E293B);

  /// Slate-700 Glass Border (`0x80334155`)
  static const Color divider = Color(0x80334155);

  /// Helper colors
  static const Color cardBorder = Color(0x80334155);
  static const Color accentRed = Color(0xFFEF4444);
  static const Color surfaceOverlay = Color(0x1FFFFFFF);
}


